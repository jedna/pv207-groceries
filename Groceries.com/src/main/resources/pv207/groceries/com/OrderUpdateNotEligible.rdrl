package pv207.groceries.com;

import java.lang.Number;

rule "OrderUpdateNotEligible"
	dialect "mvel"
	ruleflow-group "order-handling"
	when
		o : StoreOrder ( locked == false || state == "shipped" )
	then
		o.setEditable( false )
end
